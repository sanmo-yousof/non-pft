import SupporterDashboardClient from "@/app/(dashboard)/(supporter)/supporter-dashboard/_components/SupportedDashboardClient";

const page = async () => {
  return <SupporterDashboardClient />;
};

export default page;
